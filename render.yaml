services:
  - type: web
    name: scribetube-summarizer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 5000
      - key: SECRET_KEY
        generateValue: true
